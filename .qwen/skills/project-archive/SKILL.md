---
name: project-archive
description: 项目文档归档工具。将分散在 docs/<role>/ 下的产出物归档到对应项目目录 projects/<project>/docs/，确保每个项目的成果物集中管理。
---

# 项目文档归档工具

> 将分散的角色文档归档到对应项目，实现项目级文档集中管理

---

## 问题背景

当前 Auto Company 的工作流程导致：
- Agent 产出存放在 `docs/<role>/` (如 `docs/ceo/`, `docs/cto/`)
- 项目代码存放在 `projects/<project>/`
- **问题**: 项目相关文档散落在各角色目录，难以追溯

---

## 归档规则

### 目录结构对比

**归档前** (分散):
```
docs/
├── ceo/
│   ├── emailguard-pr-faq.md
│   ├── devpulse-strategy.md
│   └── image-api-decision.md
├── cto/
│   ├── emailguard-arch.md
│   └── image-api-adr.md
├── research/
│   ├── emailguard-market-analysis.md
│   └── image-api-market-analysis.md
└── ...
```

**归档后** (集中):
```
projects/
├── emailguard/
│   └── docs/
│       ├── pr-faq.md           # 来自 ceo
│       ├── architecture.md      # 来自 cto
│       ├── market-analysis.md   # 来自 research
│       └── pricing-model.md     # 来自 cfo
├── devpulse/
│   └── docs/
│       ├── strategy.md
│       └── waitlist-spec.md
└── image-api/
    └── docs/
        ├── adr.md
        ├── market-analysis.md
        └── unit-economics.md
```

### 项目关键词映射

| 项目 | 关键词 (匹配文件名) |
|------|---------------------|
| `emailguard` | emailguard, email-validation, email-validation |
| `devpulse` | devpulse, developer-monitoring, status-page |
| `image-api` | image-api, image-optimization, image-compression |
| `docuflow` | docuflow, documentation-generator |

### 角色→文档类型映射

| 角色目录 | 文档类型 | 归档后文件名 |
|----------|----------|--------------|
| `ceo/` | PR/FAQ, Strategy, Decision | `pr-faq.md`, `strategy.md`, `decision.md` |
| `cto/` | ADR, Architecture, Technical Spec | `adr.md`, `architecture.md`, `technical-spec.md` |
| `research/` | Market Analysis, Competitive | `market-analysis.md`, `competitive-analysis.md` |
| `cfo/` | Pricing, Unit Economics | `pricing-model.md`, `unit-economics.md` |
| `product/` | PRD, Spec | `prd.md`, `spec.md` |
| `ui/` | Design System, UI Spec | `design-system.md`, `ui-spec.md` |
| `devops/` | Deploy Guide, Runbook | `deploy-guide.md`, `runbook.md` |
| `marketing/` | Launch Plan, Content | `launch-plan.md`, `content-strategy.md` |
| `qa/` | Test Strategy, Bug Report | `test-strategy.md` |

---

## 执行流程

```
启动归档
     ↓
┌─────────────────────────────────────┐
│ 1. 扫描 docs/ 目录                   │
│    - 列出所有 .md 文件               │
│    - 按项目关键词分类                │
│    - 识别未关联项目的文档            │
└─────────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ 2. 匹配项目                          │
│    - 根据文件名关键词匹配项目        │
│    - 无法匹配的标记为 "general"      │
└─────────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ 3. 创建项目文档目录                  │
│    - projects/<project>/docs/        │
│    - 如不存在则创建                  │
└─────────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ 4. 移动/复制文档                     │
│    - 移动到项目文档目录              │
│    - 重命名为规范文件名              │
│    - 记录归档日志                    │
└─────────────────────────────────────┘
     ↓
┌─────────────────────────────────────┐
│ 5. 清理空目录                        │
│    - 删除空的角色目录                │
│    - 保留目录结构说明                │
└─────────────────────────────────────┘
     ↓
生成归档报告
```

---

## 使用方式

### 完整归档

```bash
# 扫描并归档所有文档
skill: "project-archive"
```

### 指定项目归档

```bash
# 只归档 emailguard 相关文档
skill: "project-archive --project emailguard"
```

### 预览模式

```bash
# 只显示将要执行的操作，不实际执行
skill: "project-archive --dry-run"
```

### 清理模式

```bash
# 归档后删除原文件
skill: "project-archive --clean"
```

---

## 归档日志

每次归档生成日志文件 `logs/archive-<timestamp>.log`:

```
2026-02-27T23:50:00Z - 开始归档
2026-02-27T23:50:01Z - 扫描到 78 个文档
2026-02-27T23:50:02Z - 匹配项目:
  - emailguard: 12 个文档
  - devpulse: 8 个文档
  - image-api: 6 个文档
  - general: 52 个文档
2026-02-27T23:50:03Z - 移动文件:
  - docs/ceo/emailguard-pr-faq.md → projects/emailguard/docs/pr-faq.md
  - docs/cto/emailguard-arch.md → projects/emailguard/docs/architecture.md
  - ...
2026-02-27T23:50:10Z - 归档完成
```

---

## 特殊处理

### cycle_*.md 文件

周期日志文件 (如 `cycle_0001_2026-02-26_13-45-01.md`) 不归档：
- 保留在 `docs/<role>/` 作为执行记录
- 或移动到 `logs/cycles/` 统一管理

### general 文档

无法匹配项目的文档：
- 保留在 `docs/general/` 
- 或移动到 `docs/archived/`

### 冲突处理

当目标文件已存在时：
- 询问用户选择：覆盖 / 跳过 / 合并
- 或使用 `--force` 强制覆盖

---

## 输出格式

```
┌─────────────────────────────────────────────────────────────┐
│  📦 项目文档归档报告                                         │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  扫描文档: 78 个                                             │
│  归档成功: 26 个                                             │
│  保留原位: 52 个 (周期日志/通用文档)                         │
│                                                              │
│  ═══ 按项目统计 ═══                                         │
│                                                              │
│  📁 emailguard: 12 个文档归档                               │
│     ├── docs/ceo/emailguard-pr-faq.md → pr-faq.md           │
│     ├── docs/cto/emailguard-arch.md → architecture.md       │
│     └── ...                                                  │
│                                                              │
│  📁 devpulse: 8 个文档归档                                  │
│     └── ...                                                  │
│                                                              │
│  📁 image-api: 6 个文档归档                                 │
│     └── ...                                                  │
│                                                              │
│  ═══ 保留在原位 ═══                                         │
│                                                              │
│  📄 周期日志: 45 个 (docs/*/cycle_*.md)                      │
│  📄 通用文档: 7 个 (无法匹配项目)                            │
│                                                              │
│  [查看详情] [清理空目录]                                    │
└─────────────────────────────────────────────────────────────┘
```

---

## 后续维护

### 新增文档规则

后续 Agent 产出应直接写入项目目录：

```
projects/<project>/docs/
├── pr-faq.md           # CEO 产出
├── architecture.md     # CTO 产出
├── market-analysis.md  # Research 产出
└── pricing-model.md    # CFO 产出
```

### 更新 PROMPT.md

在 PROMPT.md 中添加文档存放规则：

```markdown
## 文档存放规则

Agent 产出文档应存放在对应项目目录：
- 项目相关文档: `projects/<project>/docs/`
- 通用公司文档: `docs/general/`
- 周期执行日志: `logs/cycles/`

禁止在 `docs/<role>/` 下存放项目相关文档。
```

---

## 示例执行

```
用户: 帮我归档项目文档

AI: 我来执行文档归档...

1. 扫描 docs/ 目录...
   - 发现 78 个文档文件
   - 15 个角色目录

2. 匹配项目...
   - emailguard: 12 个文档
   - devpulse: 8 个文档  
   - image-api: 6 个文档
   - 周期日志: 45 个 (不归档)
   - 通用文档: 7 个 (保留)

3. 创建项目文档目录...
   - projects/emailguard/docs/ ✓
   - projects/devpulse/docs/ ✓
   - projects/image-api/docs/ ✓

4. 移动文档...
   - docs/ceo/emailguard-pr-faq.md → projects/emailguard/docs/pr-faq.md
   - docs/cto/emailguard-arch.md → projects/emailguard/docs/architecture.md
   - ...共 26 个文件移动成功

5. 归档完成！
```